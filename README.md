# LEVSCLOADES v1
Dark Windows Debloater & Optimizer dashboard based on the agreed design.

This build deliberately avoids fake success states. It queries real Windows data, opens real Windows settings/tools, and reports command return codes.

Run on Windows:
python -m pip install -r requirements.txt
python main.py

Build:
build.bat
